[project]
name = "linker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-docx",
    "Pillow",
    "langchain-anthropic",
    "langchain-google-genai",
    "python-dotenv",
    "ipywidgets",
]
